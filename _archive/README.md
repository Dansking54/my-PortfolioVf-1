# _archive — old stuff, NOT used by the live site

Nothing in here is needed to build or run the website. It's kept only as a
backup so nothing is lost. Safe to ignore.

- **`design-drafts/`** — the original Claude Design export drafts (old versions
  of the pages, `site.css`/`site.js`, the `lib/image-slot.js` design-tool
  helper, `.thumbnail`). The *real* build source lives in
  `../Dans_logs Portfolio/site/`.
- **`screenshots/`** — development screenshots taken while building.
- **`uploads/`** — raw image uploads from the design tool.

If you ever want any of these back in play, just move them out — they're plain
files, untouched.
